package com.signalapp.tradingsignalapp.Transaction;

public class Transaction {
    // Implement id, user_id, ?currency_id?, quantity, price, action (sell/buy)

    // TO string is good for debugging cause we cant see object just by printing it
    public String toString() {
        return String.format("TODO");
    }

}
